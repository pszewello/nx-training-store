import { apiUtilNotifiactions } from './api-util-notifiactions';

describe('apiUtilNotifiactions', () => {
  it('should work', () => {
    expect(apiUtilNotifiactions()).toEqual('api-util-notifiactions');
  });
});
